## Sample tools for export Excel VBA modules with text files.

### Directions

1. Input excel vba files (like .xlsm) to `Excel` directory.
2. Run `ExportVBA.bat`
3. Export results are in the `VBA` directory.
